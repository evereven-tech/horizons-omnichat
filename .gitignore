# Global .gitignore file 
# Created by https://www.toptal.com/developers/gitignore/api/terraform
# Edit at https://www.toptal.com/developers/gitignore?templates=terraform

#
# Database backups
# #############################################################################

/backups/
/backups*.sql

# IDE Environment variables
# #############################################################################

.kiro
.idea
.vscode
.aider*

# Environment files & examples
# #############################################################################

# negated patterns: exclusions
!.env.example
!*/.env.example
!**/.env.example
!terraform.tfvars.example

# common: .env
.env.aws
.env*

# Hybrid: temp & config
hybrid/tmp
hybrid/config.json
hybrid/old_config.json
hybrid/litellm_config.yaml
hybrid/litellm_image_config.yaml

# AWS: Terraform backend configuration
aws/backend.hcl
 

# Documentation
# #############################################################################
**/.infracost/*
**/.jekyll-cache/*
_site

# Terraform
# #############################################################################

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# .tfstate files
*.tfstate
*.tfstate.*

tfplan

# Local .terraform directories
**/.terraform/*

# Crash log files
crash.log
crash.*.log

# Ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
.terraform.lock.hcl
